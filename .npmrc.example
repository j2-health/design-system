# Copy this file to .npmrc and replace j2-health with your actual GitHub organization
# Replace ${GITHUB_TOKEN} with your actual token OR set it as an environment variable

@j2-health:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

